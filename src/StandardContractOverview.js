import React from "react";

export const StandardContractOverview = () => (
  <div className="contracts">
    <p>
      Each contract will have a quick summary of what it is then detailed, API specifications A NEAR
      Enhancement Proposal (NEP) is a design document that is used to provide information to the
      NEAR community or describe a new feature for the NEAR protocol or Smart Contract standards.
    </p>
  </div>
);
